services:
  - type: web
    name: nostro-lingua-lab
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/session
    envVars:
      - key: ADMIN_PASSWORD
        generateValue: true
